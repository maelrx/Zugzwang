# Analysis plan: EXP-ID

## Frozen inputs

- bundle IDs:
- corpus hash:
- condition fingerprints:
- metric versions:
- query/code commit:

## Data integrity checks

- pairing completeness;
- duplicate/transposition audit;
- provider/model drift;
- protocol violations;
- missing artifacts;
- declared/effective H/K mismatch;
- outlier/failure reason distribution.

## Primary estimator

Define the paired contrast at PositionFamily/Episode unit, not only aggregate means.

## Uncertainty

- cluster bootstrap unit;
- confidence/credible interval;
- randomization inference when applicable;
- sensitivity to engine metric variants.

## Secondary and heterogeneous effects

Predeclare phase, tacticality, branching, familiarity and model-family interactions. Label any additional slice exploratory.

## Multiplicity

Describe family of hypotheses and correction/hierarchical strategy. Avoid treating every model-condition-slice cell as an independent headline.

## Cost/latency

Report calls, input/output tokens, provider-reported usage, estimated cost status and wall time. Unknown is not zero.

## Figures/tables

List planned outputs before running analysis.

## Robustness

- exclude protocol violations;
- intention-to-treat including failures;
- alternative evaluator budget;
- first-attempt versus committed action;
- public-redacted bundle equivalence.

## Claim mapping

Each conclusion maps to a Claim Register row and evidence tier.
