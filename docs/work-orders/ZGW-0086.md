# Work order ZGW-0086: matriz de modelos padrão (GATE-011) e default experimental de memória

- Objetivo: registrar a ratificação do GATE-011 pelo operador (matriz muse-spark-1.3 via opencode free+Go, visão nativa) e tornar memória persistente o default experimental para full-game; sincronizar os artefatos de decisão.
- Autoridade: Mestre Mael, decisão direta em 2026-09-06 ("padrão inicial opencode based plano free e Go, apenas muse-spark-1.3-contributor ou free até esgotar a cota; tem visão nativa, sem mimo nem deepseek; memória persistente vira default por ter produzido a vitória e as partidas mais longas").
- Milestone: M6 suite científica; destrava a execução paga da suite 0.1 dentro da cota da assinatura.
- Requisitos: NFR-005 (rastreabilidade), ADR-046 (observação multimodal), ADR-047 (K0–K7), política de preregistro (amendments timestamped).
- Gates: GATE-011 aceito nesta ordem. GATE-005/007/008/009/010/012 permanecem pendentes (nenhum claim USD — GATE-009).

## Caminhos permitidos

`docs/decisions/DECISIONS.yaml`, `docs/decisions/DECISION_LOG.md`,
`docs/decisions/HUMAN_DECISION_GATES.pt-BR.md`, `docs/adr/ADR-046-observacao-multimodal.md` (emenda),
`docs/research/PREREGISTRATION_SUITE_0_1.md` (Emenda 002),
`docs/research/EXPERIMENT_CARD_SUITE_0_1.md` (Emenda 002),
`experiments/research-suite-0.1/generate_manifests.py` e os 14 manifests regenerados,
esta ordem.

## Execução

1. GATE-011 → accepted com a matriz do operador (muse-spark-1.3-contributor, fallback `-free`, router `127.0.0.1:8788`, perfil `openai-responses` — o padrão validado overnight).
2. Gerador da suite: corpus `positions_v1_1` (1.1.0, conforme Emenda 001) + matriz muse-spark; regenerar os 14 manifests e validar cada um.
3. Emenda 002 da suite: troca de matriz pré-execução; mesma-modelo em texto e visão resolve o confundimento RGB da Emenda 001; memória persistente vira default experimental para full-game.
4. Console de gates sincronizado com `DECISIONS.yaml` (6 aceitos: 001/002/003/004/006/011).

## Evidência

- `uv run zugzwang experiment validate` em todos os manifests regenerados.
- `uv run python scripts/validate_foundation.py --strict` e `scripts/decision_status.py`.
- Suíte offline `pytest -m "not e2e"` verde.

## Limites e stop conditions

- Nenhuma execução paga é disparada por esta ordem; o piloto é lançado em ordem separada com orçamento registrado.
- Nenhum claim em USD (GATE-009 pendente); custo reportado como `unknown` com usage real.
- Fallback `-free` é troca manual do campo `model` no manifest quando a cota Go esgotar; não altera hipóteses.
- Se a validação dos manifests regenerados falhar por contrato, parar e reportar (não contornar schema).
