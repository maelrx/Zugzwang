# Work order ZGW-0098: CB-WO-11 — primeira ablação preregistrada

```yaml
work_order:
  id: ZGW-0098
  title: "CB-WO-11: primeira ablação — condições congeladas, análise offline, relatório com custo"
  objective: "Executar o CB-WO-11 do PRD (§38.12): medir contribuição perceptiva
    e interativa — condições congeladas, conjuntos, scripts de análise e
    relatório com custo. Plano estatístico definido antes da análise final;
    sem reivindicar rating de GM com um piloto."
  milestone: "Fase 6 — Ablação (caminho §38.1; W8 → W11)"
  priority: P1
  requirements:
    - "PRD §38.12 + TEST-071 a TEST-074, route freeze, dados completos"
    - "PRD §36 CB3: métricas e protocolo (TEST-066 já coberto; 071–074 aqui)"
  gates_required: []
  skill: "code-review (pós-implementação)"
  allowed_paths:
    - docs/work-orders/ZGW-0098.md
    - docs/research/ABLATION_CB_01.md
    - scripts/analyze_cb_ablation.py
    - tests/unit/test_cb_ablation.py
    - docs/roadmap/COGNITIVEBOARD-CHECKLIST.md (via branch do PR #26)
  forbidden_paths:
    - packages/zugzwang-chess/ packages/zugzwang-core/ packages/zugzwang-runtime/
    - packages/zugzwang-cli/ plugins/ schemas/ experiments/ datasets/
    - viewer-next/
  dependencies: [ZGW-0095 (journal/audit como fonte de dados) — branch empilhada]
  acceptance:
    - "TEST-071: perspectiva correta; mate fora da média de CP."
    - "TEST-072: diferença negativa preservada e sinalizada, sem erro de sinal."
    - "TEST-073: condições usam mesmas posições, splits e versões (pareamento)."
    - "TEST-074: trabalho formal pré-computado entra no custo."
    - "Route freeze: análise declara rota/versões congeladas."
    - "Plano estatístico antes da análise final (prereg na WO + doc)."
    - "Relatório com custo (unknown quando sem preço — GATE-009)."
  evidence_required:
    - "uv run pytest -m 'not e2e' verde"
    - "uv run ruff check . / format --check verdes"
    - "uv run python scripts/validate_foundation.py --strict"
  stop_conditions:
    - "Nenhum claim de rating GM ou superioridade com piloto."
    - "Nenhuma execução paga/ rede / engine em teste default."
    - "Nenhuma mudança em código de produção (docs + scripts + testes)."
  reviewer_role: "human (Mestre Mael) via PR"
```

## Contexto

Primeira ablação do CognitiveBoard: memória ON vs OFF sobre decisões
journaled offline — o script lê pares de snapshots/audits (condições
congeladas: mesmas posições, splits, versões, rota) e computa diferenças de
métrica com perspectiva correta, custo contabilizado e sinal preservado. Tudo
offline e determinístico; o "experimento" aqui é o protocolo + análise sobre
fixtures, não uma bateria paga.

## Delta planejado

1. `docs/research/ABLATION_CB_01.md` — prereg: condições, conjuntos, plano
   estatístico, route freeze, relatório-modelo com custo.
2. `scripts/analyze_cb_ablation.py` — análise offline determinística
   (pareamento, perspectiva, CP sem mate na média, sinal, custo).
3. `tests/unit/test_cb_ablation.py` — TEST-071..074 sobre fixtures.

## Handoff

```yaml
status: pending
changed_paths: []
tests: {passed: [], skipped: [], failed: []}
requirements_satisfied: []
invariants_checked: []
decisions_needed: []
risks: []
follow_up: []
```
