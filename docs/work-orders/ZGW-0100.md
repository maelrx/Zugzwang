# Work order ZGW-0100: CB-WO-13 — planos e premissas

```yaml
work_order:
  id: ZGW-0100
  title: "CB-WO-13: planos e premissas — continuidade estratégica entre turnos"
  objective: "Executar o CB-WO-13 do PRD (§38.14): continuidade estratégica
    entre turnos — plano condicional, revisão por delta e provenance. DSL
    pequena de predicados sobre premissas false/unknown."
  milestone: "Fase 5 — Planos (caminho §38.1; W9 → W13)"
  priority: P1
  requirements:
    - "PRD §38.14 + TEST-048/075 e fixtures de premissa false/unknown"
    - "PRD §43.2 CB-M4 (DDL de investigações e planos transcrito)"
  gates_required: []
  skill: "code-review (pós-implementação)"
  allowed_paths:
    - docs/work-orders/ZGW-0100.md
    - packages/zugzwang-runtime/src/zugzwang_runtime/persistence/migrations/versions/0010_cb_m4_plans.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/cognition/plans.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/cognition/__init__.py
    - tests/integration/test_cb_plans.py
    - docs/roadmap/COGNITIVEBOARD-CHECKLIST.md (via branch do PR #26)
  forbidden_paths:
    - packages/zugzwang-chess/ packages/zugzwang-core/ packages/zugzwang-cli/
    - plugins/ schemas/ experiments/ datasets/
    - viewer-next/
  dependencies: [ZGW-0096 (CB-M2 revisões) — branch empilhada]
  acceptance:
    - "TEST-048: premissa alterada marca needs_review (plano), sem declarar
       refutação estratégica."
    - "TEST-075: plano entre turnos com perspectiva/episódio corretos e
       premissas atualizadas."
    - "Fixtures de premissa false/unknown: predicados avaliam os três estados."
    - "DSL de predicados pequena e auditável (sem execução automática de
       sequência predefinida)."
  evidence_required:
    - "uv run pytest -m 'not e2e' verde"
    - "uv run ruff check . / format --check / pyright verdes"
    - "uv run python scripts/validate_foundation.py --strict"
  stop_conditions:
    - "Sem certificado de refutação estratégica."
    - "Sem execução automática de sequência predefinida."
    - "Sem UPDATE/DELETE em planos/investigações fora das transições."
    - "Sem rede/secrets/engines em teste default."
  reviewer_role: "human (Mestre Mael) via PR"
```

## Contexto

CB-M4 fecha o ciclo memória→planos: investigações ancoradas em nós de decisão
e planos condicionais por episódio/perspectiva, com premissas avaliadas por
uma DSL mínima (false/unknown/true) e revisão por delta. O plano nunca executa
sozinho e nunca declara refutação — apenas marca needs_review.

## Delta planejado

1. Migration `0010_cb_m4_plans.py` — DDL §43.2 (investigations, plans +
   episode guard; triggers de imutabilidade alinhados).
2. `runtime/cognition/plans.py` — `PlanStore` (open/update por delta,
   predicados, revisão) — sem auto-execução.
3. `tests/integration/test_cb_plans.py` — TEST-048/075 + fixtures false/unknown.

## Handoff

```yaml
status: pending
changed_paths: []
tests: {passed: [], skipped: [], failed: []}
requirements_satisfied: []
invariants_checked: []
decisions_needed: []
risks: []
follow_up: []
```
