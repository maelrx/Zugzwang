# Work order ZGW-0101: CB review fixes — integração e critérios de aceite (stack #25–#38)

```yaml
work_order:
  id: ZGW-0101
  title: "Reabertura da entrega CognitiveBoard — correção de integração e
    critérios de aceite contra o PRD v0.1 (snapshot a2d0241)"
  objective: "Corrigir os bloqueadores encontrados pela revisão independente
    da stack #25–#38 no head a2d0241, tomando o PRD
    Zugzwang_PRD_CognitiveBoard_v0.1.md (sha256
    488fe38c594645ebeed4bd8233954e3b81ba17de755558e39d0dfec946657d4d) como
    critério de aceite: expansão real do broker, loop produtivo sobre o
    ModelBackend, contratos/IDs de providers, budgets/transação/recuperação,
    memória/snapshots/skills/planos, viewer e bateria de testes."
  milestone: "Revisão da stack CB-WO-01..13 (PRs #25–#38)"
  priority: P0
  requirements:
    - "PRD §4 (FR-001..040), §11 (tools), §12 (loop/strategy), §13 (orçamento),
       §14 (durabilidade), §15 (erros), §16 (memória), §36 (catálogo TEST),
       §37 (quality gates)"
    - "Gates humanos G-CB-01..08 preservados; CB-WO-14/15 fora do escopo"
  gates_required: []
  skill: "code-review (pós-implementação)"
  allowed_paths:
    - docs/work-orders/ZGW-0101.md
    - docs/roadmap/COGNITIVEBOARD-CHECKLIST.md (via branch do PR #26)
    - packages/zugzwang-runtime/src/zugzwang_runtime/cognition/
    - packages/zugzwang-runtime/src/zugzwang_runtime/fakes/
    - packages/zugzwang-runtime/src/zugzwang_runtime/search/workspace.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/execution/coordinator.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/execution/durable_coordinator.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/application/durable_services.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/persistence/cognition.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/persistence/migrations/versions/0009_cb_m3_skills.py
    - packages/zugzwang-runtime/src/zugzwang_runtime/application/bundles.py
    - packages/zugzwang-chess/src/zugzwang_chess/cognition/perception.py
    - packages/zugzwang-chess/src/zugzwang_chess/strategies/cognitive_navigation.py
    - plugins/provider-openai-compatible/
    - scripts/build_cognitive_viewer.py
    - scripts/analyze_cb_ablation.py
    - viewer-next/src/
    - tests/
  forbidden_paths:
    - main (nenhum merge/auto-merge); worktrees de outros agentes
    - packages/zugzwang-core/src/zugzwang_core/ports/ (contratos estáveis)
  dependencies: [ZGW-0087..ZGW-0100 (stack #25–#38), branch empilhada]
  acceptance:
    - "TEST-005/006/009/014/022/023/024 reais: filho retornado pelo broker é
       observável e expansível; feedback causal no request seguinte; finalize
       validado na raiz real"
    - "Run completo offline por CLI/coordinator com ModelBackend fake
       (golden): filho+neto, seleção antecipada, memória ON/OFF observável,
       restart no meio, guardas de rede"
    - "Nenhum COMMITTED sem débito/exposição; resume reconstrói estado;
       kill/restart em processo novo nos pontos de falha"
    - "Matriz requisito→implementação→teste→evidência→status honesta por WO"
  evidence_required:
    - "uv sync --all-packages --all-extras --locked"
    - "uv run ruff check . / uv run ruff format --check . / uv run pyright"
    - "uv run pytest -m 'not e2e'"
    - "uv run python scripts/validate_foundation.py --strict"
  stop_conditions:
    - "Sem merge em qualquer PR; sem push em main; heads da stack congelados"
    - "Sem inferência paga, engine live ou experimento científico live"
    - "Sem ratificação silenciosa de gates humanos (G-CB-01..08 intocados)"
    - "PRD permanece untracked (apenas hash registrado na evidência)"
    - "Sem exclusão de dados/worktrees; banco local .zugzwang intocado"
  reviewer_role: "human (Mestre Mael) via PR"
```

## Contexto

A revisão independente da stack #25–#38 no head `a2d0241` encontrou
bloqueadores em sete trilhas. Este work order cobre a correção; o PRD fica
como critério de aceite (versão/hash na evidência, arquivo fora do repo).

## Delta planejado (um commit por trilha, teste de regressão antes do patch)

1. **T1 — broker `_expand` real**: integração SearchWorkspace + RulesKernel;
   resolução de `action_id` sobre o conjunto legal completo (todas as
   páginas); transição; estado-filho registrado (snapshot + binding +
   `search_nodes`); nó/aresta; trajetória/profundidade/root_action/término;
   `child_node_id`/`state_key` no resultado; filho observável e expansível.
2. **T2 — loop produtivo**: `run()` chama `ModelBackend.infer` por round com
   transcript real; scripts movidos para o fake backend; strategy
   `chess.cognitive_navigation` selecionável nos coordinators; sessão de
   decisão aberta pelo composition root; golden offline pelo fluxo real.
3. **T3 — contratos/IDs/providers**: IDs originais de tool calls preservados;
   validação de schema do resultado antes de COMMITTED; capability gate via
   `infer`; composição completa por MockTransport.
4. **T4 — budgets/transação/recuperação**: unidades distintas (model calls,
   operações lógicas, queries físicas, profundidade, estados); reserva final
   de inferência; settle+exposição+debito atômicos; sem ValueError/PREPARED
   órfão; resume reconstruindo; kill/restart em processo novo; smokes
   rotulados (nada research-ready).
5. **T5 — memória/snapshots/skills/planos**: `_eligibility` sem atalho de
   partition=test; restore sem defaults inventados; migration 0009 com
   gatilhos NULL/status completos e downgrade recusado antes de mudar estado;
   memória/skills/planos no request efetivo.
6. **T6 — viewer/análise**: leitura read-only real do banco-fonte;
   CognitiveTimeline roteada com bundle real; ablação com totais separados;
   engine fora da superfície live.
7. **T7 — testes/entrega**: golden CLI, guardas de rede, CI reproduzida,
   checklist do PR #26 e handoffs corrigidos, plano de restack.
