# Work order ZGW-0102: Match Desk SOTA como viewer oficial + sistema de aparência

```yaml
work_order:
  id: ZGW-0102
  title: "Tornar a experiência 'Research workspace' do viewer-next o viewer
    oficial, com temas/texturas/escala de fonte, e remover a linhagem
    anterior de camadas L0/L1/L2"
  objective: "Substituir a UI de camadas do viewer-next (App L0/L1/L2,
    CognitiveTimeline) pela experiência de biblioteca de execuções
    (biblioteca, breadcrumb, Partida/Análise/Evidências, Comparar) ratificada
    por Mestre Mael em 2026-09-06, adicionando sistema de aparência (temas
    Escuro/Profundo/Claro, texturas de papel SVG, escala de fonte) e pipeline
    de snapshot ao vivo (public/data + regen a cada 30 s)."
  milestone: "M5, manutenção e rastreabilidade; observabilidade local"
  authority: "Mestre Mael por diretiva direta em chat (2026-09-06): a UI SOTA
    é a oficial; remover resquícios da linhagem anterior do repo e do remoto."
  requirements: ["NFR-005", "NFR-015", "NFR-025"]
  adrs: ["ADR-030", "ADR-031", "ADR-062"]
  gates: ["GATE-005 (snapshots privados não versionados)", "GATE-011 (matriz muse-spark-1.3, inalterado)"]
  allowed_paths:
    - "viewer-next/**"
    - "docs/work-orders/ZGW-0102.md"
    - "docs/engineering/REPOSITORY_STATUS.md"
  context: |
    A experiência SOTA foi criada em 2026-09-05 num workspace efêmero em /tmp
    (branch codex/zgw-webui-experience, nunca publicada) e perdida no reboot
    de 2026-09-06. Foi reconstruída integralmente a partir dos transcripts de
    sessão (~/.codex/sessions/2026/09/05) + estado 591b28f + ground-truth de
    leituras de arquivo, e validada com tsc/build limpos e 9/9 testes de
    presentation.test.mjs. A linhagem anterior (App em camadas,
    CognitiveTimeline/cognitive.ts de ZGW-0097/CB-WO-10) é substituída; os
    dados cognitivos permanecem no banco e na análise oficial
    (scripts/analyze_deep.py), apenas a UI é trocada.
  stop_conditions: ["nenhum dado privado versionado (GATE-005)", "nenhum teste offline quebrado"]
```

## Escopo e critérios de aceite

- `viewer-next` entrega a experiência de biblioteca ratificada, sem
  referências vivas à linhagem de camadas (CognitiveTimeline/cognitive.ts
  removidos; README atualizado).
- Sistema de aparência funcional: temas (Escuro padrão, Profundo ≈ −25% de
  luminosidade, Claro), texturas SVG data-URI sem dependência nova, escala de
  fonte 90–130%, persistência local (localStorage `zg-appearance`).
- Dados ao vivo: `viewer-next/scripts/regen-snapshot.sh` publica
  `public/data/data.json`; o app faz polling a cada 15 s; `/data` é servido
  pelo próprio Vite (proxy 4173 removido nesta branch).
- `npx tsc -b`, `npx vite build` e
  `node --experimental-strip-types --test tests/presentation.test.mjs`
  passam (9/9).
- Remotos: branch publicada via PR; nenhum snapshot privado no push (GATE-005).

## Fora de escopo

- Remoção do `viewer/` legado (M0): continua alvo padrão de
  `scripts/build_readonly_viewer.py`; decisão própria de work order.
- Mudanças no kernel Python, schemas ou contratos públicos.
