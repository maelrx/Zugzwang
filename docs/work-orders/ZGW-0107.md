# ZGW-0107 — High-Potential Gemini 3.8 Flash Low Battery (AGY Adapter)

## Status

In progress (2026-09-08). Operator directive (Mestre Mael):
1. Recover all context and memories from the previous session.
2. Analyze all new test round results, including the historic Luna checkmate victory against Stockfish 1320.
3. Consolidate and re-analyze all data with Stockfish depth-20 evaluation.
4. Launch 3 high-potential full-game runs with the `provider.antigravity_cli` adapter using `gemini-3.8-flash-low`, incorporating the lessons validated in the Luna and Gemini fullgame victories.

## Context & Synthesis

- **Luna Victory**: In ZGW-0105 WS-5 (`zgx-luna-05-tactical-memory`), `gpt-5.6-luna` (high) achieved a clean 33-move (65 plies) checkmate victory (`33. Rb7#`) against Stockfish 1320 (20k nodes). White ACPL was 55 cp with only 1 blunder (at ply 55 when up +16 material). The critical driver was eliminating the artificial "finalize by Round 2" constraint, expanding search depth to 6 rounds, enabling `inline_child_packages: true`, `preload_root: true`, and `reasoning_memory: factual`.
- **Gemini Track Record**: In earlier fullgame runs (`experiments/zgx/fullgames/`), `gemini-3.8-flash-low` already achieved two fullgame checkmate victories:
  - `zgx-full-01-tactical-memory`: Mate in 135 plies (`68. Qb6#`).
  - `zgx-full-03-king-safety-history`: Mate in 61 plies (`31. Rh7#`).
- **Luna Guardian Peak**: In Luna WS-1 (`zgx-luna-01-guard`), Luna reached a peak advantage of +5.00 against Stockfish 1320 through meticulous tactical candidate expansion, outplaying Stockfish before a late-game blunder at ply 51.

## The 3 High-Potential Setups

1. **`zgx-gemini-01-tactical-memory-v2`**:
   Porting the Luna champion setup (`zgx-luna-05`) directly to Gemini 3.8 Flash Low via AGY adapter: 6 rounds, unconstrained finalize on candidate survival, `inline_child_packages: true`, factual memory, 12-ply history.
2. **`zgx-gemini-02-king-safety-prophylaxis-v2`**:
   Upgrading the fastest winning Gemini setup (`zgx-full-03`, 61-ply mate) with 6 rounds, `inline_child_packages: true`, factual memory, and unconstrained verification.
3. **`zgx-gemini-03-tactical-guardian-v2`**:
   Porting the Tactical Guardian setup from Luna WS-1 (+5.00 peak) with explicit anti-hanging and response verification checks to prevent late-game blunders.

## Requirements

- R1: Save and freeze all empirical evidence (Luna 01-05 PGNs, Stockfish d20 evals, and Muse R2-01 PGN/eval) under `out/luna_fullgames/` and `out/muse_r2/`.
- R2: Create 3 new manifests in `experiments/zgx/fullgames5/`:
  - `zgx-gemini-01-tactical-memory-v2.yaml`
  - `zgx-gemini-02-king-safety-prophylaxis-v2.yaml`
  - `zgx-gemini-03-tactical-guardian-v2.yaml`
- R3: Execute the 3 runs in isolated workspaces (`/tmp/gemini-ws-1..3`) to avoid SQLite concurrency locks, with live Stockfish evaluation and logging.
- R4: All tests and validations pass offline standards.

## Gates

- G1: Schema and manifest validation clean (`zugzwang experiment validate`).
- G2: Execution initiated and monitored with real `provider.antigravity_cli` calls.
- G3 (human): Operator review of game progression and final outcomes.
