# ZGW-0109 — Jogar: tabuleiro funcional e interface enxuta

Status: complete. Autoridade: pedido direto do Mestre Mael em 2026-09-09.

## Escopo e requisitos

Manutenção local do viewer oficial (ZGW-0102), sobre a arena ZGW-0108.
R1: tabuleiro criado na primeira carga, clique/arrasto legal e promoção.
R2: estados de espera/erro claros, retomada e polling resistente a falhas.
R3: tabuleiro em destaque, setup legível e opções avançadas recolhidas.
R4: navegação Jogar correta sem alterar a biblioteca.

Allowed paths: viewer-next/src/components/PlayView.tsx, PlayView.css,
viewer-next/src/App.tsx, docs/work-orders/ZGW-0109.md.
Base: worktree isolada de 21d1807 com snapshot dos arquivos de arena do
viewer ativo. Preservar alterações de outros agentes e os dados privados.
ADRs: ADR-004 (regras), ADR-036 (captura privada), ADR-040 (export bloqueado).
Gates: nenhum novo gate aceito; sem publicação ou mudança de protocolo.
Stop: alteração no kernel, dados brutos, orçamento ou setup científico.
Evidência: build e testes do viewer, suíte offline existente e inspeção no
navegador do tabuleiro, lances, retomada, navegação e layout estreito.

## Handoff — 2026-09-09

Aplicado no viewer ativo em 127.0.0.1:4190, com readback SHA-256 dos três
arquivos idêntico à worktree isolada. Nenhuma dependência adicionada.
O proxy e os arquivos de arena existentes são baseline do agente anterior.
Não foi alterado o backend, os setups nem as evidências anteriores.

Correções: lifecycle do tabuleiro na primeira carga; clique e arrasto;
orientação dos nomes; trava de turno; retry só no turno pendente do modelo;
polling volta após falha e descarta respostas obsoletas; retomada por aba;
menu e comparação; formulário e layout responsivo com opções avançadas.
Desistência fica indisponível enquanto o modelo pensa para evitar corrida
com o backend atual. Promoção preservada, sem novo teste visual específico.

```yaml
status: complete
changed_paths:
  - viewer-next/src/App.tsx
  - viewer-next/src/components/PlayView.tsx
  - viewer-next/src/components/PlayView.css
  - docs/work-orders/ZGW-0109.md
tests:
  passed:
    - tsc -p tsconfig.app.json --tsBuildInfoFile /tmp/zgw-app.tsbuildinfo
    - tsc -p tsconfig.node.json --tsBuildInfoFile /tmp/zgw-node.tsbuildinfo
    - vite build --configLoader runner
    - npm test
    - npm run lint
    - pytest tests/unit/test_arena_play.py tests/integration/test_arena_server.py -q (12 passed)
    - pytest -m 'not e2e' -q (513 passed, 1 skipped, 8 deselected)
    - UI real Gemini, clique e4 e arrasto Nf3; respostas e5 e Nc6
    - Recarga preservou partida; navegação Comparar e Jogar
    - Viewport 390x844 sem overflow horizontal no modo Jogar
    - Console do navegador sem erros
  skipped:
    - Um teste marcado pela suíte; oito e2e deselecionados
    - Validação real de outros providers e promoção no navegador
  failed:
    - Tentativa inicial de tsc/vite bloqueada por cache compartilhado read-only; resolvida com cache em /tmp e configLoader runner
    - Tentativa inicial dos testes HTTP bloqueada pelo sandbox; repetição autorizada passou
requirements_satisfied: [R1, R2, R3, R4]
invariants_checked: [legalidade server-side, evidencias preservadas, sem mudanca de protocolo, sem novas dependencias]
decisions_needed: []
risks:
  - Build informa chunk 3D preexistente acima de 500 kB
  - Disponibilidade dos providers depende dos servicos locais existentes
follow_up: []
```
