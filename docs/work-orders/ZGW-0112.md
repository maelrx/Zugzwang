# ZGW-0112 — Corrigir reconstrução de histórico recortado

Autoridade: Mestre Mael, 2026-09-09, erro concreto no Jogar.
Status: complete.
Requisitos: FR-022/023/029; preservar todos os lances, evidências e setup.
ADRs: 004/058/062. Nenhum gate alterado. Manutenção do substrato M2 usado
pela arena local existente. Sem alteração de contrato, assistência ou protocolo.
Allowed paths: packages/zugzwang-chess/src/zugzwang_chess/cognition/perception.py,
tests/unit/test_zgw0103_packet_exposure.py, este documento.
Skill: diagnosing-bugs; implement-chess-environment não está instalada neste
host/repositório (não executada). Não há arquitetura ou API nova nesta correção.
Stop: sequência persistida ilegal, chamadas ambíguas, conflitos de edição.
Reprodução: load_game da arena-20260909-014800-5806 seguida de
board.packet(board.state, 'n0') produz exatamente AssertionError f8f7.


## Causa e correção

A janela last_n era recortada antes do replay, que então aplicava seu primeiro
lance sobre a posição inicial. A sequência completa era legal e permanecia
imutável; o erro ocorria no preload antes de inferência. Agora o prefixo omitido
é reproduzido em tabuleiro temporário antes de codificar a janela. O provider
continua recebendo exatamente N entradas e os mesmos índices; nenhuma expansão
de exposição ou assistência. SAN passa a usar o contexto correto.

Reprodução original passou para as duas partidas afetadas (25/24 e 13/12 plies).
Dez regressões adicionais: janelas 1/2/3/24 × UCI/SAN, mais FEN customizado com
promoção × UCI/SAN; comparação com a cauda do histórico completo e imutabilidade.
Primeira execução vermelha: 7 falhas, incluindo a AssertionError exata; após a
correção: 17 testes do módulo passaram. Expectativa SAN corrigida para Qxh5,
a captura realmente executada na partida, conforme replay legal completo.

Serviço 4191 reiniciado sem turnos ativos e com ambiente anterior preservado;
8 jogos/contagens conferidos. Backup local em /tmp/zgw-0112-arena-before.
Retomada única do turno com falha pré-inferência: Luna respondeu 13...f5;
26 plies, last_error=null e turno das brancas. UI verificada em Jogar com
“Sua vez de jogar”. Evidências JSONL anteriores preservadas como prefixo.
Não se alteraram outras partidas nem resultados/artefatos científicos antigos.

```yaml
status: complete
changed_paths:
  - packages/zugzwang-chess/src/zugzwang_chess/cognition/perception.py
  - tests/unit/test_zgw0103_packet_exposure.py
  - docs/work-orders/ZGW-0112.md
tests:
  passed:
    - regression module (17 passed)
    - pytest -m 'not e2e' -q (528 passed; 4 socket setup errors retried)
    - pytest tests/integration/test_arena_server.py -q (4 passed, loopback allowed)
    - ruff check and format on changed Python files
    - pyright -p /tmp/zgw-0112-pyright.json (strict; explicit existing site-packages; zero errors)
    - original saved-game packet reproduction (both affected games)
    - live Luna recovery and browser readback (26 plies, human turn)
  skipped:
    - 1 pytest skip and 8 deselected e2e tests
  failed:
    - pre-fix regression (7 failures, expected)
    - initial full-suite socket setup (4 errors; all passed on retry)
    - default pyright could not resolve installed chess; resolved using temporary config
requirements_satisfied: [FR-022, FR-023, FR-029]
invariants_checked:
  - no illegal canonical moves applied
  - saved move prefix unchanged and retry did not duplicate a committed ply
  - previous raw call evidence remains an identical prefix
  - no gate or experimental exposure changes
  - no new dependency or public contract
  - unrelated dirty files and worktrees preserved
decisions_needed: []
risks:
  - historical runs may have encountered the same history-window defect; not rewritten
follow_up: []
```
