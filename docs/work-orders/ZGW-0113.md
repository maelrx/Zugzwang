# ZGW-0113 — Bateria luna-high em modo Fast (Codex `service_tier=fast`)

## Status

In progress (2026-09-09). Diretiva do operador (Mestre Mael): rodar a bateria
completa das novas propostas do estudo de correlação (ZGW-0108) usando luna
high no modo fast do Codex CLI.

## Base empírica (estudo ZGW-0108)

- Config campeã: rounds 6 + inline_child + preload_root + noascii + hist 12
  uci + reasoning_memory factual + diretivo tático (estrato 3W–0L, ACPL 46.9).
- Diretivos v2 a isolar: TACTICAL PRIORITIES & FACTUAL MEMORY (3W–1L em 2
  modelos) e TACTICAL GUARDIAN (melhor ACPL 61.0/mB 1.5); KING SAFETY v2 como
  controle (perdeu para gemini, 164 plies).
- Luna high: único modelo com vitória limpa 0 blunders (luna-05).

## Requisitos

- R1: `provider.codex_cli` ganha `service_tier` configurável via
  `backend_config` (flag `-c service_tier=<tier>`); default = não enviar (o
  CLI herda o config local, hoje `default`). O tier efetivo segue na
  evidência wire (o CLI pode fazer downgrade — nunca presumir fast).
- R2: wiring em `durable_services` (bloco codex) + teste de contrato (flag
  presente quando configurado, ausente quando não).
- R3: 3 manifests em `experiments/zgx/fullgames6/` (luna high, fast):
  1. `zgx-luna-fast-01-tactical-memory` — diretivo tactical-memory v2
  2. `zgx-luna-fast-02-king-safety-prophylaxis` — diretivo king-safety v2 (controle)
  3. `zgx-luna-fast-03-tactical-guardian` — diretivo tactical-guardian v2
  Todos: gpt-5.6-luna, effort high, timeout 420, max_tokens 32768, r6,
  preload, inline_child, factual, hist 12 uci, ascii off, brancas vs SF1320
  20k nós, max_plies null.
- R4: validação offline limpa (`experiment validate`, suíte offline, lint,
  pyright nos arquivos tocados).
- R5: 3 runs concomitantes em workspaces isolados `/tmp/luna-fast-ws-1..3`
  (codex não usa o router; padrão ZGW-0105) + watcher SF-d20 por run.

## Gates

- G1: validação offline limpa.
- G2: 3 runs iniciados com chamadas reais luna/fast (tier efetivo anotado na
  evidência).
- G3 (humano): revisão dos resultados quando os jogos decidirem.

## Não-objetivos

- Tool `board_threats` computada (proposta separada, em aberto).
- Mudança de esforço/modelo; cobrança premium do fast é aceita pelo
  operador (informada: pode sofrer downgrade para default).
