# ZGW-0115 — Seleção Fast para Luna na arena

Status: complete. Autoridade: Mestre Mael, pedido direto 2026-09-09.
Dependências: adapter Codex service_tier da ZGW-0113 e painel ZGW-0114.
R1: escolher Padrão/Fast apenas no GPT-5.6 Luna; R2: persistir a escolha e
passar service_tier=fast ao adapter existente; R3: rejeitar tier em outros
modelos/provedores e preservar o padrão e raciocínio escolhidos.
Allowed paths: arena/providers.py, arena/server.py; tests/unit/test_arena_fast.py;
viewer-next/src/components/{PlayView.tsx,ChessDesk.css}; viewer-next/src/lib/arena.ts;
este documento. ADR-062 e escopo local ZGW-0108, sem novo gate/contrato core.
QA fake somente; manter tokens/custos desconhecidos sem promessas de preço.
Stop: edição concorrente ou turno ativo durante reinício.


## Entrega

Catálogo anuncia service_tiers=[fast] somente no GPT-5.6 Luna. Campo aditivo
service_tier em setup é null no padrão, fast quando escolhido. A seleção é
validada antes da criação, persistida e encaminhada ao adapter já existente;
effort permanece independente. Troca de modelo/provedor limpa Fast no formulário.
Modelo ao lado do tabuleiro mostra o sufixo Fast nas partidas criadas nesse modo.
O modo solicitado não garante a fila efetiva do provedor nem altera seus limites.

QA: seleção de Codex → Luna mostra Padrão/Fast; Astra/Gemini não mostram o
campo; selecionar Fast, trocar para Astra e voltar ao Luna restaura Padrão;
criação pela UI fake persistiu gpt-5.6-luna + service_tier=fast e exibiu Fast
junto do oponente. Catálogo e seleção também verificados no viewer real 4190.
Não iniciada partida real para QA. Oito partidas existentes preservadas no
reinício; nenhuma análise/turno estava ativo. Backups em /tmp/zgw-0115-before
(código) e /tmp/zgw-0115-games-before (dados).

```yaml
status: complete
changed_paths:
  - packages/zugzwang-cli/src/zugzwang_cli/arena/providers.py
  - packages/zugzwang-cli/src/zugzwang_cli/arena/server.py
  - tests/unit/test_arena_fast.py
  - viewer-next/src/components/PlayView.tsx
  - viewer-next/src/components/ChessDesk.css
  - viewer-next/src/lib/arena.ts
  - docs/work-orders/ZGW-0115.md
tests:
  passed:
    - focused Fast/progress/provider tests (28 passed)
    - pytest -m 'not e2e' -q (550 passed)
    - strict pyright on arena/provider (zero errors)
    - ruff check/format on changed Python files
    - TypeScript app compile
    - npm run lint
    - vite build --configLoader runner
    - browser selection/reset/create and persisted fake-game readback
    - live provider catalog and UI readback
  skipped:
    - 1 pytest skip; 8 deselected e2e
    - real Fast provider call during QA
  failed: []
requirements_satisfied: [ZGW-0115-R1, ZGW-0115-R2, ZGW-0115-R3]
invariants_checked:
  - Fast only offered/accepted for Luna
  - default tier preserved for all other combinations
  - model/effort selection unchanged by tier
  - all real games preserved
  - existing adapter Fast implementation preserved
  - no gate changes or paid QA calls
decisions_needed: []
risks:
  - effective latency/tier depends on provider availability
  - preexisting Board3D chunk-size and python-chess warnings remain
follow_up: []
```
