# ZGW-0118 — Nova bateria segura dos setups vencedores

Status: complete (implementation and launch; experiments continue). Authority: Mestre Mael, 2026-09-09: recontextualize project,
force-stop existing runs, rerun most promising winners with Luna high Fast,
five concurrent runs and live webapp.
Requirements: FR-014, FR-051, immutable private evidence, explicit H/K.
ADRs: ADR-063, ADR-058; GATE-003/006 accepted; this specific batch is authorized
without changing general GATE-011 model matrix or public redistribution gates.
Allowed paths: experiments/zgx/isolated-winners, out/isolated-winners-20260909,
live snapshot publisher and existing viewer provider availability fields.
Five runs: tactical memory r6 (Luna/Gemini winning lineage), tactical guardian r6,
tactical memory r4 (R1 winner), king-safety history r4 (R1 winner), persistent H3
single-agent-tree (historical Muse winner). All use Luna high Fast and model-only/v1.
Preserve source protocol/assistance and declare differences (provider and isolation,
new identity; H3 removes prior 120-ply censor as historical winning setup ran to end).
Budget ceiling: source max_calls per run (4x3000 + 600 = 12600), max_attempts 1;
no hidden fallback, no posthoc engine information supplied to the model.
Stop on isolation failure, quota failure, ambiguous provider failure or source drift.
No outcome claims before natural completion. Live means committed moves and status
from fresh workspace snapshots, not simulated progress.

Operator budget override, before launch: "Não precisa de limite, todas partidas
rodando até o final mate ou empate". Explicitly authorizes all five Luna high Fast
runs without artificial call/ply/time ceilings. Quota/isolation/infrastructure
failures still stop; no automatic credit purchase or reset consumption authorized.
Live auth metadata verified auth_mode=chatgpt, API key absent, paid credits=0.
This is a scoped execution authorization, not a global model-matrix change.

First attempt failed before remote inference: strict config rejected legacy
model_max_output_tokens (unknown field previously silently ignored). Preserved all
five failed workspaces/ledgers and original manifests. Removed this unsupported
output cap as operator requested no limit; new -cli-fixed condition names and
-v2 workspaces. Actual high/Fast CLI adversarial proof now passes with tools=[].
Canonical guard additionally latches isolation failures across strategy retries
and checks the latch before committing, preventing swallowed security exceptions.

H3 alone failed capability preflight before provider calls (0 attempts): Codex
adapter had not implemented JSON_SCHEMA_OUTPUT. Added the CLI-supported
--output-schema lowering with an inert schema file; exact H3 schema tested against
real CLI/fake server, tools remain empty. Other four runs stay running untouched.
H3 gets a new v3 workspace; v2 failure remains included in live history.
