# Local real-provider smoke through the ZCode/OpenCode loopback router.
# The router owns upstream authentication; this manifest intentionally sends
# no provider key and records the run privately in the local workspace.
api_version: zgw.dev/v1alpha1
kind: Experiment

metadata:
  name: local-musespark-1-3-free-smoke
  tags: [local, free-provider, openai-compatible, move-selection]

spec:
  seed: 20260904

  task:
    plugin: chess.tasks
    version: "0.1"
    config:
      kind: move-selection
      start_fen: rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1
      backend_config:
        base_url: http://127.0.0.1:8788/v1
        provider_id: opencode-router
        timeout_seconds: 120
        profile: openai-responses
        allow_private_network: true
        reasoning_effort: minimal
        default_max_output_tokens: 2048

  players:
    white:
      model:
        backend: provider.openai_compatible
        provider: opencode-router
        model: muse-spark-1.3-contributor-free
        strategy: chess.direct
    black:
      policy:
        plugin: fake.stay

  protocol:
    declared_assistance: H2
    declared_knowledge: K0
    observation:
      position: {fen: true, ascii: false}
      side_to_move: true
      move_number: true
      history: {mode: none}
    retries:
      transport: 0
      parse: 0
      illegal: 0

  budget:
    max_calls: 1
    max_concurrent_episodes: 1
    max_attempts: 1

  artifacts:
    raw_requests: true
    raw_responses: true
    redact: standard
