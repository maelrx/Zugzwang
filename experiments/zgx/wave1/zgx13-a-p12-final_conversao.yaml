# ZGX-13 onda 1 — N6-neutro | p12-final_conversao (final_conversao) | BA
# Posição-fonte: cb-pilot-resume8:resume8@ply20. Par pareado; esforço LOW; rota muse congelada.
# controle N6 prompt neutro.
api_version: zgw.dev/v1alpha1
kind: Experiment

metadata:
  name: zgx13-a-p12-final_conversao
  tags: [zgx, wave1, real, zgx13, final_conversao]

spec:
  seed: 202690011

  task:
    plugin: chess.tasks
    version: "0.1"
    config:
      cognitive_interaction_mode: native_tools
      kind: move-selection
      start_fen: rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1
      move_prefix: [e2e4, b7b6, d2d4, b8c6, d4d5, c8b7, d5c6, f7f6, c6b7, d8c8, b7c8q, a8c8, g1f3, f6f5, e4f5, a7a6, f1a6, h7h6, a6c8, e7e6]
      backend_config:
        base_url: http://127.0.0.1:8788/v1
        provider_id: opencode-router
        timeout_seconds: 420
        profile: openai-responses
        allow_private_network: true
        reasoning_effort: low
        default_max_output_tokens: 8192
      cognitive:
        max_rounds: 6
        preload_root: true
        directive: >-
          Choose the best move for the side to move. Use board_observe on the
          root, investigate with board_expand as needed — an expansion returns
          the child's position package, and you may expand FROM a child to
          examine the adversary's reply. Then finalize with board_finalize on
          the ROOT node with the chosen action_id. The legal list order is not
          a quality ranking.


  players:
    white:
      model:
        backend: provider.openai_compatible
        provider: opencode-router
        model: muse-spark-1.3-contributor
        strategy: chess.cognitive_navigation
    black:
      policy:
        plugin: fake.stay

  protocol:
    declared_assistance: H4
    declared_knowledge: K0
    observation:
      position: {fen: true, ascii: false}
      side_to_move: true
      move_number: true
      history: {mode: last_n, plies: 6, notation: uci}
    retries:
      transport: 4
      parse: 1
      illegal: 4

  budget:
    max_calls: 60
    max_concurrent_episodes: 1
    max_attempts: 1

  evaluation: []

  artifacts:
    raw_requests: true
    raw_responses: true
    redact: standard
