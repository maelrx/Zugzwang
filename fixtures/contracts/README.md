# Contract fixtures

M0 will place immutable valid/invalid fixtures here for manifests, events, provider messages, metrics and bundles. Fixtures are versioned evidence for compatibility tests, not mutable demo data.
