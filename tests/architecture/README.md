# Architecture tests

M0 will implement tests that fail on forbidden import edges, SDK type leakage, CLI-to-SQL access and plugin access to internal repositories. This placeholder records the required test boundary before code exists.
